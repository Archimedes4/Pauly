import React from 'react';
import { Text } from 'react-native';

// This file should not be compiled. This only exists so that no error is given.
export default function CommissionClaim({}: {
  commission: commissionType;
  imageData?: string;
}) {
  return <Text>Uh Oh Something went wrong</Text>;
}
